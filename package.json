{
  "name": "meuapp_api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start_db": "json-server --watch backend/db.json --port 3000",
    "host": "ssh -p 443 -R0:localhost:3000 qr@a.pinggy.io",
    "start_app": "cd meuapp_api && npx expo start --tunnel && cd .."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "json-server": "^1.0.0-beta.13"
  }
}
